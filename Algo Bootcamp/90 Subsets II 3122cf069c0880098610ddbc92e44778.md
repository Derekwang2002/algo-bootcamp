# 90. Subsets II

Date: Feb 24
Level: Medium
Minutes: 15
Review Recommendation: ✅ Good job
Status: Done
Tags: Backtracking, Deduplicate
Time Complexity: O(n * 2^n)
Space Complexity: O(n)
URL: https://leetcode.com/problems/subsets-ii/
Carl's: https://programmercarl.com/0090.%E5%AD%90%E9%9B%86II.html#%E7%AE%97%E6%B3%95%E5%85%AC%E5%BC%80%E8%AF%BE

<aside>
💡

Given an integer array `nums` that may contain duplicates, return *all possible* *subsets (the power set)*.

The solution set **must not** contain duplicate subsets. Return the solution in **any order**.

</aside>

# Thought

- Similar to [**78. Subsets**](78%20Subsets%203122cf069c0880f3bd32c02588279a51.md), just need deduplication
- Deduplication design:
    - same thought as: [**40. Combination Sum II**](40%20Combination%20Sum%20II%203102cf069c088038b996d67cc42ac7f0.md)
    - presort and check when `i > idx`, continue if dup former
    
    ```java
    if (i > idx && nums[i] == nums[i - 1]) continue;
    ```
    
- Tree:

```
start idx=0, nums=[1,2,2]
path=[]           -> add []

i=0 选1
└── path=[1]      -> add [1]
    idx=1
    |
    ├── i=1 选2
    │   └── path=[1,2]        -> add [1,2]
    │       idx=2
    │       |
    │       └── i=2 选2   (i==idx, 允许)
    │           └── path=[1,2,2]  -> add [1,2,2]
    │           backtrack, remove last
    │       backtrack, remove last
    |
    └── i=2 选2   ✗ (i>idx 且 nums[2]==nums[1]，同层重复 -> continue)
    backtrack, remove 1

i=1 选2
└── path=[2]      -> add [2]
    idx=2
    |
    └── i=2 选2   (i==idx, 允许)
        └── path=[2,2]        -> add [2,2]
        backtrack, remove last
    backtrack, remove last

i=2 选2   ✗ (i>idx 且 nums[2]==nums[1]，同层重复 -> continue)

output:
[]
[1]
[1,2]
[1,2,2]
[2]
[2,2]
```

# Solution

```java
class Solution {
    List<List<Integer>> res = new ArrayList<>();
    List<Integer> path = new ArrayList<>();

    public List<List<Integer>> subsetsWithDup(int[] nums) {
        res.add(new ArrayList<>());

        Arrays.sort(nums);
        backtrack(nums, 0);
        return res;
    }

    private void backtrack(int[] nums, int idx) {
        for (int i = idx; i < nums.length; i++) {
            if (i > idx && nums[i] == nums[i - 1]) {
                continue;
            }

            path.add(nums[i]);
            res.add(new ArrayList<>(path));
            
            backtrack(nums, i + 1);
            path.remove(path.size() - 1);
        }
    }
}
```