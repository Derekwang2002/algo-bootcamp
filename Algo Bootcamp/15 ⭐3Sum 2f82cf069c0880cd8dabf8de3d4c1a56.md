# 15. ⭐3Sum

Date: Jan 29
Level: Hard
Minutes: 80
Review Recommendation: 🚩 Recommend Redo (Hard >50m)
Status: Done
Tags: DoublePointer, HashTable

<aside>
💡

Given an integer array nums, return all the triplets `[nums[i], nums[j], nums[k]]` such that `i != j`, `i != k`, and `j != k`, and `nums[i] + nums[j] + nums[k] == 0`.

Notice that the solution set must not contain duplicate triplets.

</aside>

> 梦破碎的地方！
> 
- Timer:
    - ***40min*** get correct output
    - ***30min*** pass time limit
    - ***10min*** syntax

# Thought

- **（哈希→不要使用这种解法！）**:
    - 理解起来很简单，就是在两数之和的基础上（不完全一样）再套一层循环
    - nums：存到HashMap中，值→key，index→value
    - 两层for循环i，j（类似两数之和暴力解，三角遍历）：
        - i，j自然不相等，求sum = nums[i] + nums[j]
        - 找**符合条件**的0-sum
- Evaluation:
    - 这道题，最难的点就在于“符合条件”这几个字了。为什么？因为答案要求不重复，那么就需要**去重**，去重的操作是比较复杂的
    - 此题代码量较大，在修改结构时，需要花费较多的时间
    - 设计较多的创建不同类型容器的语法，由于不够熟悉，导致查阅的过程也消耗了大量时间
- **双指针解法**：**这才是最优解答！（简单明了，效率高）**
    - Assume these three numbers are **a,b,c**, which a+b+c=0
        - We need deduplicate a, b, and c
    - To use double pointer method, the `nums` must be **sorted**
    - One pointer is **loop pointer(a)**, which moves along the loop. And the others are **left(b) and right(c) pointer**.
    - for(i in index): `nums[i]==nums[i-1]` means **duplication for ‘a’**
        - while(left<right): find compatible other two numbers for this loop pointer
        - within single loop, check the sum of nums[pointer]s,
            - if it >0, right pointer need to move left
            - if it <0, left pointer need to move right
            - if it ==0, we found one compatible triplet.
                - **deduplication for ‘b/c’**:only when found a solution(*key*)
                
                ```java
                while (right > left && nums[right] == nums[right - 1]) right--;
                while (right > left && nums[left] == nums[left + 1]) left++;
                ```
                

# Solution

```java
class Solution {
    public List<List<Integer>> threeSum(int[] nums) {
        Set<List<Integer>> set = new HashSet<>();
        Map<Integer, List<int[]>> map = new HashMap<>();

        for (int i = 0; i < nums.length; i++) {
            for (int j = i + 1; j < nums.length; j++) {
                
                int key = nums[i] + nums[j];
                map.putIfAbsent(key, new ArrayList<>());
                map.get(key).add(new int[]{i, j});
            }
        }

        for (int n = 0; n < nums.length; n++) {
            int temp = 0 - nums[n];
            List<int[]> value = map.getOrDefault(temp, new ArrayList<>());

            for (int[] i : value) {
                if (n != i[0] && n != i[1]) {
                    List<Integer> triplet = new ArrayList<>(
                        Arrays.asList(
                            nums[n], 
                            nums[i[0]], 
                            nums[i[1]]
                        )
                    );

                    Collections.sort(triplet);
                    set.add(triplet);
                }
            }
        }
        List<List<Integer>> res = new ArrayList<>(set);

        return res;
    }
}
```

## Passed

```java
class Solution {
    public List<List<Integer>> threeSum(int[] nums) {
        Set<List<Integer>> set = new HashSet<>();
        Map<Integer, Integer> map = new HashMap<>();

        for (int i = 0; i < nums.length; i++) {
            map.put(nums[i], i);
        }

        for (int i = 0; i < nums.length; i++) {
            for (int j = i + 1; j < nums.length; j++) {

                int sum = nums[i] + nums[j];
                int n = map.getOrDefault(0 - sum, -1); 
                if (
                    n + 1 != 0
                    && !Set.of(i, j).contains(n)
                    // && !set.contains(Set.of(n, i, j))
                ) {
                    // set.add(Set.of(n, i, j));
                    List<Integer> triplet = new ArrayList<>(
                        Arrays.asList(
                            nums[n], 
                            nums[i], 
                            nums[j]
                        )
                    );

                    Collections.sort(triplet);
                    set.add(triplet);
                }

            }
        }
        List<List<Integer>> res = new ArrayList<>(set);

        return res;
    }
}
```

![image.png](15%20%E2%AD%903Sum/image.png)

## 双指针！

```java
class Solution {
    public List<List<Integer>> threeSum(int[] nums) {
        List<List<Integer>> result = new ArrayList<>();
        Arrays.sort(nums);
	// 找出a + b + c = 0
        // a = nums[i], b = nums[left], c = nums[right]
        for (int i = 0; i < nums.length; i++) {
	    // 排序之后如果第一个元素已经大于零，那么无论如何组合都不可能凑成三元组，直接返回结果就可以了
            if (nums[i] > 0) {
                return result;
            }

            if (i > 0 && nums[i] == nums[i - 1]) {  // 去重a
                continue;
            }

            int left = i + 1;
            int right = nums.length - 1;
            while (right > left) {
                int sum = nums[i] + nums[left] + nums[right];
                if (sum > 0) {
                    right--;
                } else if (sum < 0) {
                    left++;
                } else {
                    result.add(Arrays.asList(nums[i], nums[left], nums[right]));
		    // 去重逻辑应该放在找到一个三元组之后，对b 和 c去重
                    while (right > left && nums[right] == nums[right - 1]) right--;
                    while (right > left && nums[left] == nums[left + 1]) left++;

                    right--;
                    left++;
                }
            }
        }
        return result;
    }
}
```