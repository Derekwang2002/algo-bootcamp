# 242. Valid Anagram

Date: Jan 27
Minutes: 20
Review Recommendation: ✅ Good job
Status: Done
Tags: HashTable

<aside>
💡

Given two strings `s` and `t`, return `true` if `t` is an anagram of `s`, and `false` otherwise.

*An anagram is a word or phrase formed by rearranging the letters of a different word or phrase, using all the original letters exactly once.*

</aside>

# 思路：

- 根据定义，就是要判断每个单词所含的各个字母的个数是否一致，与哈希表原理一致
- 用26长度的列表作为桶，用“x”-“a”（字符编码的相对值）的方式得到hash value作为index

# Solution

```python
class Solution(object):
    def isAnagram(self, s, t):
        """
        :type s: str
        :type t: str
        :rtype: bool
        """
        
        len_s = len(s)
        len_t = len(t)

        if not (len_s == len_t):
            return False

        count_s = [0] * 26
        count_t = [0] * 26

        for c in s:
            count_s[ord(c) - ord('a')] += 1
            
        for c in t:
            count_t[ord(c) - ord('a')] += 1

        if count_s == count_t:
            return True

        return False
```