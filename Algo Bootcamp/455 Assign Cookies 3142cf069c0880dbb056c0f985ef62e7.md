# 455.  Assign Cookies

Date: Feb 26
Level: Easy
Minutes: 15
Review Recommendation: ✅ Good job
Status: Done
Tags: Greedy
Time Complexity: O(nlogn)
Space Complexity: O(1)
URL: https://leetcode.com/problems/assign-cookies/description/
Carl's: https://programmercarl.com/0455.%E5%88%86%E5%8F%91%E9%A5%BC%E5%B9%B2.html#%E7%AE%97%E6%B3%95%E5%85%AC%E5%BC%80%E8%AF%BE

<aside>
💡

Assume you are an awesome parent and want to give your children some cookies. But, you should give each child at most one cookie.

Each child `i` has a greed factor `g[i]`, which is the minimum size of a cookie that the child will be content with; and each cookie `j` has a size `s[j]`. If `s[j] >= g[i]`, we can assign the cookie `j` to the child `i`, and the child `i` will be content. Your goal is to maximize the number of your content children and output the maximum number.

</aside>

# Thought

- Double Pointer
    - one on greedy factor list
    - one on cookie size list
- Presort two list
- Iterate on cookie size list(samll cookie first):
    - if current cookie can satisfy child, update child (increase)
    - underlying meaning: if current cookie cannot satisfy current child, we need to find a bigger cookie.
    - end loop when one of the pointer reach its end

# Solution

```java
// small cookie first
class Solution {
    public int findContentChildren(int[] g, int[] s) {
        int child = 0;
        int cookie = 0;
        int count = 0;

        Arrays.sort(g);
        Arrays.sort(s);

        while (cookie < s.length && child < g.length) {
            if (s[cookie] >= g[child]) {
                count++;
                child++;
            }

            cookie++;
        }

        return count;
    }
}
```

```java
// big cookie first
class Solution {
    // 思路2：优先考虑胃口，先喂饱大胃口
    public int findContentChildren(int[] g, int[] s) {
        Arrays.sort(g);
        Arrays.sort(s);
        int count = 0;
        int start = s.length - 1;
        // 遍历胃口
        for (int index = g.length - 1; index >= 0; index--) {
            if(start >= 0 && g[index] <= s[start]) {
                start--;
                count++;
            }
        }
        return count;
    }
}
```