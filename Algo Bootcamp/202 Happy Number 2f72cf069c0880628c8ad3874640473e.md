# 202. Happy Number

Date: Jan 28
Minutes: 40
Review Recommendation: ✅ Good job
Status: Done
Tags: HashTable

<aside>
💡

Write an algorithm to determine if a number `n` is happy.

A **happy number** is a number defined by the following process:

- Starting with any positive integer, replace the number by the sum of the squares of its digits.
- Repeat the process until the number equals 1 (where it will stay), or it **loops endlessly in a cycle** which does not include 1.
- Those numbers for which this process **ends in 1** are happy.

Return `true` *if* `n` *is a happy number, and* `false` *if not*.

</aside>

# Thought:

- 题目中已知重要条件：如果不是Happy number， 则会出现重复，所以在发现重复之前一直循环查找即可！
    - 使用HashSet存储已有结果
- 计算某个数的数字平方和用到了一个trick，不断取出最右数字：

```java
while (n != 0){
    int i = n % 10; // get the most right digit
    sum += i * i;
    n /= 10; // drop the most right digit
```

- Evaluation:
    - 思路简单，主要考查一个计算的trick，同时使用了哈希集合。

# Solution

## My solution

```java
class Solution {
    public boolean isHappy(int n) {

        HashSet<Integer> sums = new HashSet<>();

        while (!sums.contains(n)){
            if (n == 1) {return true;}

            sums.add(n); // must add after judgment

            int sum = 0;
            while (n != 0){
                int i = n % 10; // get the most right digit
                sum += i * i;
                n /= 10; // drop the most right digit
            }

            n = sum; // renew n
        }

        return false;
    }
}
```

## Carl’s

```java
class Solution {
    public boolean isHappy(int n) {
        Set<Integer> record = new HashSet<>();
        while (n != 1 && !record.contains(n)) {
            record.add(n);
            n = getNextNumber(n);
        }
        return n == 1; // smart way
    }

    private int getNextNumber(int n) {
        int res = 0;
        while (n > 0) {
            int temp = n % 10;
            res += temp * temp;
            n = n / 10;
        }
        return res;
    }
}
```