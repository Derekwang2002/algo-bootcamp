# 383.Ransom Note

Date: Jan 29
Level: Easy
Minutes: 20
Review Recommendation: ✅ Good job
Status: Done
Tags: HashTable
URL: https://leetcode.com/problems/ransom-note/description/

<aside>
💡

Given two strings `ransomNote` and `magazine`, return `true` *if* `ransomNote` *can be constructed by using the letters from* `magazine` *and* `false` *otherwise*.

Each letter in `magazine` can only be used once in `ransomNote`.

</aside>

# Thought

- 与 242非常像，也是用数组进行哈希
- 先将`magazine`中的字母按个数存到`mag`数组(int[26])中，index为[char - ’a’]
- 再循环`ransomNote`中的char，在`mag`中对对应字母-1，后，若值为负，则return false
- 若循环完整结束，则return true
- Evaluation：
    - 类似于“池”的模型，创建了一个字母池，然后消耗，判断结果
    - 字符串/字符 类题可联想 数组哈希

# Solution

```java
class Solution {
    public boolean canConstruct(String ransomNote, String magazine) {
        int[] mag = new int[26];

        for (char c : magazine.toCharArray()){
            mag[c - 'a'] += 1;
        }

        for (char c : ransomNote.toCharArray()){
            mag[c - 'a'] -= 1;
            if (mag[c - 'a'] < 0){return false;}
        }

        return true;

    }
}
// +++++ Another way to loop char in string +++++
// ==============================================
// for (int i = 0; i < s.length(); i++) {
//     char c = s.charAt(i);
// }
```