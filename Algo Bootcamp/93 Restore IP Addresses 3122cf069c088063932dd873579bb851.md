# 93. Restore IP Addresses

Date: Feb 24
Level: Medium
Minutes: 90
Review Recommendation: 🚩 Recommend Redo (Medium >40m)
Status: Review-logic
Tags: Backtracking, Math, String
Time Complexity: O(3^4)
Space Complexity: O(n)
URL: https://leetcode.com/problems/restore-ip-addresses/description/
Carl's: https://programmercarl.com/0093.%E5%A4%8D%E5%8E%9FIP%E5%9C%B0%E5%9D%80.html#%E7%AE%97%E6%B3%95%E5%85%AC%E5%BC%80%E8%AF%BE

<aside>
💡

A **valid IP address** consists of exactly four integers separated by single dots. Each integer is between `0` and `255` (**inclusive**) and cannot have leading zeros.

- For example, `"0.1.2.201"` and `"192.168.1.1"` are **valid** IP addresses, but `"0.011.255.245"`, `"192.168.1.312"` and `"192.168@1.1"` are **invalid** IP addresses.

Given a string `s` containing only digits, return *all possible valid IP addresses that can be formed by inserting dots into* `s`. You are **not** allowed to reorder or remove any digits in `s`. You may return the valid IP addresses in **any** order.

</aside>

# Thought

- Drop cases in main function that input length not in [4, 12]
- Backtracking recursion design(*A lot edge conditions!*)
    - Virtually cutting like in [131. Palindrome Partitioning](131%20Palindrome%20Partitioning%203102cf069c0880c6a098f8fe425d7c77.md)
    - Title: return void, take `idx` as start index, `remain` as #integer left for IP
    - Base case: remain equals 1, do actions, then return
        - why not remain == 0? - reduce unnecessary recursion
        - Actions: if rest of the `s` is a valid integer for IP, append it to path and store path in `res`
    - Steps: loop substrings ()
        - in what **range**? - do math (*upper bound could greater than 3, because any number has 4 digits will greater than 255, which will be trimmed at validation check immediately*)
        
        ```java
        int min = Math.max((s.length() - idx) - 3 * (remain - 1) - 1, 0); // min len
        for (int i = idx + min; i < idx + n - (remain - 1); i++)
        ```
        
        - check if < 256
        - append to path and do backtracking
        - if start with 0 then break (because need add 0 to path first)
- Tree:
    
    ```
    (idx=0, remain=4)  rest=25525511135
    |
    └── 255.
        (idx=3, remain=3)  rest=25511135
        |
        └── 255.
            (idx=6, remain=2)  rest=11135
            |
            ├── 1.
            │    (remain=1)  last=1135 ✗ (>255)
            |
            ├── 11.
            │    (remain=1)  last=135 ✓
            │    => 255.255.11.135
            |
            ├── 111.
            │    (remain=1)  last=35 ✓
            │    => 255.255.111.35
            |
            └── 1113 ✗  (break >255)
    ```
    

# Solution

## Mine (probably best)

```java
class Solution {
    List<String> res = new ArrayList<>();
    StringBuilder path = new StringBuilder();

    public List<String> restoreIpAddresses(String s) {
        if (s.length() < 4 || s.length() > 12) return res;
        backtrack(s, 0, 4);
        return res;
    }

    private void backtrack(String s, int idx, int remain) { // 4 -- 12
        int n = s.length() - idx; // rest
        
        // base case
        if (remain == 1) {
            String sub = s.substring(idx, s.length());
            if (n == 1 || (s.charAt(idx) != '0' && Integer.parseInt(sub) < 256)) {
                path.append(sub);
                res.add(path.toString());
            }
            return;
        }

				// conquer
				int min = Math.max(n - 3 * (remain - 1) - 1, 0);
				// optional max = Math.min(n - (remain - 1), 3);
        for (int i = idx + min; i < idx + n - (remain - 1); i++) {
            String sub = s.substring(idx, i + 1);
            if (Integer.parseInt(sub) > 255) {
                break;
            }

            int len = path.length();
            path.append(sub);
            path.append('.');

            // divide
            backtrack(s, i + 1, remain - 1);
            path.setLength(len);

            if (s.charAt(idx) == '0') {
                break;
            }
        }
    }
}
```

![image.png](93%20Restore%20IP%20Addresses/image.png)

- 代码随想录中Java代码质量过低，无参考价值

## GPT finetunning

```java
**import java.util.*;

class Solution {
    List<String> res = new ArrayList<>();
    StringBuilder path = new StringBuilder();

    public List<String> restoreIpAddresses(String s) {
        int n = s.length();
        if (n < 4 || n > 12) return res;
        backtrack(s, 0, 4);
        return res;
    }

    private void backtrack(String s, int idx, int remain) {
        int n = s.length();
        int rest = n - idx;

        // 长度剪枝：剩余字符必须够 remain 段，每段 1~3 位
        if (rest < remain || rest > 3 * remain) return;

        // base case：最后一段，直接校验并加入结果
        if (remain == 1) {
            int len0 = path.length();

            int val = 0;
            for (int i = idx; i < n; i++) {
                val = val * 10 + (s.charAt(i) - '0');
                if (val > 255) {
                    path.setLength(len0);
                    return;
                }
            }

            // 前导 0：长度>1 且首位为 0 不合法
            if (n - idx > 1 && s.charAt(idx) == '0') {
                path.setLength(len0);
                return;
            }

            path.append(s, idx, n);
            res.add(path.toString());
            path.setLength(len0);
            return;
        }

        // 尝试取 1~3 位作为一段
        int len0 = path.length();
        int val = 0;

        for (int end = idx; end < n && end < idx + 3; end++) {
            val = val * 10 + (s.charAt(end) - '0');
            if (val > 255) break;

            // 追加本段 + '.'
            path.append(s, idx, end + 1).append('.');

            backtrack(s, end + 1, remain - 1);

            // 回溯
            path.setLength(len0);

            // 前导 0：如果这一段以 0 开头，只能取单个 '0'
            if (s.charAt(idx) == '0') break;
        }
    }
}**
```