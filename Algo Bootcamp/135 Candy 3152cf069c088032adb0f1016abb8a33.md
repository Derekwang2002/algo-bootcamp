# 135. Candy

Date: Feb 28
Level: Hard
Minutes: 120
Review Recommendation: 🚩 Recommend Redo (Hard >50m)
Status: Review-logic
Tags: Greedy
Time Complexity: O(n)
Space Complexity: O(n)
URL: https://leetcode.com/problems/candy/description/
Carl's: https://programmercarl.com/0135.%E5%88%86%E5%8F%91%E7%B3%96%E6%9E%9C.html#%E7%AE%97%E6%B3%95%E5%85%AC%E5%BC%80%E8%AF%BE

<aside>
💡

There are `n` children standing in a line. Each child is assigned a rating value given in the integer array `ratings`.

You are giving candies to these children subjected to the following requirements:

- Each child must have at least one candy.
- Children with a higher rating get more candies than their neighbors.

Return *the minimum number of candies you need to have to distribute the candies to the children*.

</aside>

# Thought

> **Failed** to come up a valid greedy strategy!
> 
- Greedy strategy:
    - Forward traverse → only deal with right side rating is bigger
        - store this-time result in int[] `candies`
    - Backward travers ← only deal with left side rating is bigger
        - final result = choose max of `candies[i+1]` and this-time result.
- Implement(a single traverse):
    - this-time result:
        - if current rating is bigger than the previous one
        - then current candy is previous candy + 1
    - sum up within the second traverse

# Solution

```java
class Solution {
    public int candy(int[] ratings) {
        int res = 0;

        int len = ratings.length;
        int[] candies = new int[len];

        for (int i = 0; i < len; i++) {
            if (i - 1 >= 0 && ratings[i] > ratings[i - 1]) {
                candies[i] = candies[i - 1] + 1;
            } else {
                candies[i] = 1;
            }
        }

        for (int i = len - 1; i >= 0; i--) {
            int candidate = 1;

            if (i + 1 < len && ratings[i] > ratings[i + 1]) {
                candidate = candies[i + 1] + 1;
            }

            candies[i] = Math.max(candidate, candies[i]);
            res += candies[i];
        }

        return res;
    }
}
```

### Carl’s

```java
class Solution {
    /**
         分两个阶段
         1、起点下标1 从左往右，只要 右边 比 左边 大，右边的糖果=左边 + 1
         2、起点下标 ratings.length - 2 从右往左， 只要左边 比 右边 大，此时 左边的糖果应该 取本身的糖果数（符合比它左边大） 和 右边糖果数 + 1 二者的最大值，这样才符合 它比它左边的大，也比它右边大
    */
    public int candy(int[] ratings) {
        int len = ratings.length;
        int[] candyVec = new int[len];
        candyVec[0] = 1;
        for (int i = 1; i < len; i++) {
            candyVec[i] = (ratings[i] > ratings[i - 1]) ? candyVec[i - 1] + 1 : 1;
        }

        for (int i = len - 2; i >= 0; i--) {
            if (ratings[i] > ratings[i + 1]) {
                candyVec[i] = Math.max(candyVec[i], candyVec[i + 1] + 1);
            }
        }

        int ans = 0;
        for (int num : candyVec) {
            ans += num;
        }
        return ans;
    }
}
```