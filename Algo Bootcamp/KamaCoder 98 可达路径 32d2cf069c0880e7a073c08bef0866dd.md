# KamaCoder 98. 可达路径

Date: Mar 24
Level: Medium
Minutes: 40
Review Recommendation: ✅ Good job
Status: Done
Tags: GraphTheory
Time Complexity: O(V!)
Space Complexity: O(V + E)
URL: https://kamacoder.com/problempage.php?pid=1170
Carl's: https://programmercarl.com/kamacoder/0098.%E6%89%80%E6%9C%89%E5%8F%AF%E8%BE%BE%E8%B7%AF%E5%BE%84.html#%E6%8F%92%E6%9B%B2

<aside>
💡

给定一个有 n 个节点的有向无环图，节点编号从 1 到 n。请编写一个函数，找出并返回所有从节点 1 到节点 n 的路径。每条路径应以节点编号的列表形式表示。

- input: 第一行包含两个整数 N，M，表示图中拥有 N 个节点，M 条边。后续 M 行，每行包含两个整数 s 和 t，表示图中的 s 节点与 t 节点中有一条路径。
- output: 输出所有的可达路径，路径中所有节点之间空格隔开，每条路径独占一行，存在多条路径，路径输出的顺序可任意。如果不存在任何一条路径，则输出 -1。**注意输出的序列中，最后一个节点后面没有空格！** 例如正确的答案是 `1 3 5`,而不是 `1 3 5 `， 5后面没有空格！
</aside>

# Thought

- DFS for DAG
- Store input → adjacent table
- LeetCode: https://leetcode.com/problems/all-paths-from-source-to-target/

# Solution

```java
import java.util.*;

public class Main{
    static List<Integer> path;
    static ArrayList<Integer>[] graph;
    static boolean pass = false;

    public static void main(String[] args) {
        Scanner in = new Scanner(System.in);

        int vertices = in.nextInt();
        int edges = in.nextInt();

        graph = new ArrayList[vertices + 1];
        for (int i = 1; i <= vertices; i++) {
		        graph[i] = new ArrayList<>();
		    }

        while(in.hasNext()) {
            graph[in.nextInt()].add(in.nextInt());
        }

        path = new ArrayList<>();
        path.add(1);
        
        dfs(1, vertices);

        if (!pass) System.out.print(-1);
    }

    private static void dfs(int v, int target) {
        if (v == target) {
            pass = true;
            
            StringBuilder sb = new StringBuilder();
            for (int i = 0; i < path.size(); i++) {
                if (i > 0) sb.append(' ');
                sb.append(path.get(i));
            }
            
            System.out.println(sb);
            return;
        }

        for (int cur : graph[v]) {
            path.add(cur);
            dfs(cur, target);
            path.remove(path.size() - 1);
        }
    }
} 
```